<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shuo.coresource">

    <application
        android:allowBackup="true"
        android:icon="@drawable/coresource_icon"
        android:label="CoResource"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConsumeSomeActivity"></activity>
        <activity android:name=".ConsumedResourcesActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".AddResourceActivity" />
        <activity android:name=".ResourceDetailActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>